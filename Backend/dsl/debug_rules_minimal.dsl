# Minimal Debug Configuration
# ===========================
# This configuration allows all rules except explicitly denied ones

# Only deny dangerous operations
deny: code_restructure
deny: full_rewrite

# Basic settings
optimize_efficiency: false
max_patches: 5
